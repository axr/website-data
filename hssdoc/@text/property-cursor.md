The `auto` value corresponds to:

- `textX` when the text is selectable (by default)
- `prohibit` when the dragged text can't be dropped
- `drop` when the dragged text is currently over a valid target
- `pointer` if an [@mouse](/@mouse) event is attached to the text's container
