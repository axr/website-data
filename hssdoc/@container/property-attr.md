It permits to add attributes or reset the values of existing ones.

	// language=hss
	// @value accepts custom properties
	attr: @value { name: "string"; };
	// which is equivalent to
	attr.name: "string";
