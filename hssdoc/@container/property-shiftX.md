It permits to shift horizontally the element(s) matched in relation to their
current position.

    // language=hss
    element
    {
        // shifts all the element(s) by 100 HSS points to the right
        shiftX: 100;
    }
